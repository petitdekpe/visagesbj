<div class="admin-bar">
    <div class="container admin-bar__inner">
        <div class="admin-bar__links">
            <a href="{{ path('admin_personnalites_index') }}">Personnalités</a>
            <a href="{{ path('admin_personnalites_new') }}">+ Ajouter</a>
            <a href="{{ path('admin_settings_edit') }}">Paramètres</a>
            <a href="{{ path('admin_users_index') }}">Utilisateurs</a>
        </div>
        <div class="admin-bar__links">
            <a href="{{ path('admin_profile_edit') }}">Mon profil</a>
            <a href="{{ path('app_home') }}">Voir le site</a>
            <form method="post" action="{{ path('admin_logout') }}">
                <input type="hidden" data-controller="csrf-protection" name="_csrf_token" value="{{ csrf_token('logout') }}">
                <button type="submit" class="admin-bar__logout">Déconnexion</button>
            </form>
        </div>
    </div>
</div>
